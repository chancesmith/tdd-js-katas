// TDD Kata 0.1 - ListNames
// take an array of employees and return array of names
// Example: ["Sally", "Tim",...]

const data = require("./employees.js");

describe("ListNames", () => {
  test("", () => {
    // first test
    // console.log({ data });
    // expect(true).toBe(true);
  });
});
